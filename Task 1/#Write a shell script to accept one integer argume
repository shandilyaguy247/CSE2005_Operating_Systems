#Write a shell script to accept one integer argument and print its multiplication table.

echo "Enter a number"
read n
i=0
while [$i -le 10]
do
	echo "$n x $i = `expr $n \* $i`"
	i = `expr $i + 1`
done